#include "main.h"

/**
 * print_alphabet - a function which prints all alphabet in lowercase
 * Return: void
 */
void print_alphabet(void)
{

	char g;

	for (g = 'a'; g <= 'z'; g++)
	{
		_putchar(g);
	}
	_putchar('\n'); }
