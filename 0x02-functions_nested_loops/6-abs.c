#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @m: integer input number to evaluate
 *
 * Return: the absolute value of n
 */
int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (-m);
}
